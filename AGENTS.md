# AGENTS

## Local Rules

- Runtime logs must be written to a local file in the current working directory.
- The current runtime log file is `speak-it.log`.
- After every code change, compile the project immediately.
